[project]
name = "expert-cli"
version = "0.1.0"
description = "CLI wrapper for Expert Elixir Language Server"
requires-python = ">=3.10"

[project.scripts]
expert-cli = "expert_cli.cli:main"

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
